/* cpp program to return 42
    - Need to check how we can emit the IRs
    - Need to check if we can use the IR and feed
    duirectly to clang to get the binary file
    - Run using llc <ll compiler>
    - Run using lli <ll interp[reter]> */
int main() { return 42; }